<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sndoc.e_coffeee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light">

        <activity android:name=".user"></activity>
        <activity android:name=".settings2" />
        <activity android:name=".risaralda" />
        <activity android:name=".chiapasPasco" />
        <activity android:name=".montecillos" />
        <activity android:name=".chiapas" />
        <activity android:name=".settings" />
        <activity android:name=".coffee_activity" />
        <activity android:name=".Register"/>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>